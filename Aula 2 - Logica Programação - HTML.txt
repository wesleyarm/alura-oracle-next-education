One + Oracle

<meta charset="UTF-8"> // obrigatario para aparecer acentos
charset atributo

<script>
alert("Jvs recebe um parametro")
</script>

Escrever html no mundo js
<script>
document.write("A idade do Flávio é ");
</script>
document aceita numeros ou tipo textos
Tipo texto = string "vou passar uma string"

Contanecao com texto = texto

Varivel sempre declarado com letra minuscula

function = criar uma função // mostra - pulaLinha - etc
function pulaLinha(){
 document.write("<br>");
}

Math.random()      = numero random
+ Math.round(media))

var recolherInformacao = prompt("Pego a informação do usuario");

var reecolherNumero = parseInt(prompt("Recolher numero em especifico"));

if = se

maior >    | if pontos > 28
menor <    | if pontos < 28
igual ==   | if pontos == 28
diferente != | if pontos != 28

if(pontos > 28){
mostra("Seu time está melhor do que o ano passado")
}

if(pontos < 28){
mostra("Seu time está pior do que o ano passado")
}

// tambem posso colocar um true or false dentro do if
if(true){
mensagem
}

// if dentro de if  //

if(imc >= 18.5) {         // maior ou igual

   if(imc <= 35) {        // menor ou igual

       mostra("Seu IMC está excelente!")
   }
}

// forma correta do E

if(imc >= 18.5 && imc <= 35) {
     mostra("Seu IMC está excelente!);
}

else = se nao for | else dentro do if

if(chute == numeroPensado){
   mostra("Você acertou");
} else {
   mostra("Você errou, o número pensado foi :") + numeroPensado);
}

//    console.log(numeroPensado);       exibir informação no console

while instrução de repetição

    var anoCopa = 1930;

    while(anoCopa <= 2016) {
        mostra("Teve copa em " + anoCopa);
        anoCopa = anoCopa + 4;
    }

// while dentro do else
    var numeroPensado = sorteia();

    var chute = parseInt(prompt("Já pensei. Qual você acha que é?"));

    if(chute == numeroPensado) {

        mostra("Uau! Você acertou, pois eu pensei no " + numeroPensado);
    } else {
    
    var mgsErro = 1;

    while(mgsErro <= 10) {
        mostra("Já era, você errou!");
        mgsErro = mgsErro + 1;
        }
    }

///
    while(numeroInicial <= numeroFinal){

        if(numeroInicial == 33){
        numeroInicial = numeroInicial + 1;
        }
            else if(numeroInicial == 37){
                numeroInicial = numeroInicial + 1;
            }
        else {
            mostra("Número " + numeroInicial);
            pulaLinha();
            numeroInicial = numeroInicial + 1;
        }
    }

    mostra("Fim");

/// for = repetição ao lado do while
// for(espaco1; espaco2; espaco3){
}

// diferença for e while
    for(var multiplicador = 1; multiplicador <=10; multiplicador++){
        mostra(7 * multiplicador);
    
}

    var multiplicador = 1;

    while(multiplicador <= 10){
        mostra(7 * multiplicador);
        multiplicador = multiplicador + 1;
    }

// atalho para implementar ++ / multiplicador++

// break;      =    quebra o loop

// var numero = parseInt("12.13");COPIAR CÓDIGO
O valor de numero será 12. Para que as casas decimais sejam mantidas, usamos o parseFloat():

// var numero = parseFloat("12.13");COPIAR CÓDIGO
O valor de numero será 12.13.

// <input/> entrada de dados

// <button> Botão </button>

// receber o dado do input fora do script, dentro do html
<script>
    var input = document.querySelector("input");
    input.value

</script>

// fazer o botao funcionar
    var button = document.querySelector("button");
    button.onclick = verifica;

//  input.value = value seria o conteudo do texto // receber o valor dentro dele

// limpar caixinha de texto
        input.value = "";

// focos na caixinha de texto após digitar
         input.focus();

// array [] = sempre defnido por exemplo [0, 5, 5] - para guardar varios valores

// no javascript o primeiro elemento é 0

// length consegue verificar todo o conteudo dentro do array []
   for(var posicao = 0; posicao < segredos.lenght; posicao++) {

// push incrementa valor ao array
ex: var segredos = [{id: 1, linha: 1, },];
    segredos.push(Math.round(Math.random() * 10));

// outro exemplo empurrando valores
var frutas = ["abacaxi", "banana", "melão"];
frutas.push("abacate");


--------

// no mundo html <canvas width="600" height="400"> </canvas>
// width = largura // height = altura

// usar pincel e conectar o canvas
 var tela = document.querySelector("canvas");
 var pincel = tela.getContext("2d");
    pincel.fillStyle = "green";
    pincel.fillRect(0, 0, 600, 400);
		600 largura
		400 altura


// preencha um renatangulo
  				   pincel.fillStyle = "green";
// preencha um retangulo   pincel.fillRect(0, 0, 200, 400);

// começar um caminho - exemplo para triangulo     
    pincel.fillStyle = "yellow";
    pincel.beginPath();   // começar um caminho


// iniciando o triangulo     pincel.beginPath();
// primeira linha inicial    pincel.moveTo(100, 175);
// segunda linha iniciando   pincel.lineTo(100, 350);

// Preencher o trangulo      pincel.fill();

// para traçar um circulo    pincel.arc(300, 200, 50, 0, 2 * 3.14);

// adicionando bordas no quadrado
// define a cor do Stroke, por padrão é black  
   pincel.strokeStyle = "black";

// posicao da borda    pincel.strokeRect(0, 0, 50, 50);

// funcão com css
function desenhaQuadrado(x, y, cor) {

    var canvas = document.querySelector("canvas");
    var pincel = canvas.getContext("2d");

    pincel.fillStyle = cor;
    pincel.fillRect(x, y, 50, 50);
    pincel.strokeStyle = cor;
    pincel.strokeRect(x, y, 50, 50);

    }

    desenhaQuadrado(0, 0, "blue");

//  exemplo while com css
    var x = 0;
    while(x < 600) {

    desenhaQuadrado(x, 0, "blue");
    x = x + 50;

    }

// exemplo for com css
    for(var x = 0; x < 600; x = x + 50) {

    desenhaQuadrado(x, 0, "blue");

    }

// escrever uma função com texto
                          function desenhaTexto(x, y, texto) {
                          var tela = document.querySelector('canvas');
                          var pincel = tela.getContext('2d');

 // fonte utilizada       pincel.font='15px Georgia';
                          pincel.fillStyle='black';
                          pincel.fillText(texto, x, y);    
    }


//   function desenhaCirculo(evento) {

        var x = evento.pageX - tela.offsetLeft;
        var y = evento.pageY - tela.offsetTop;
        pincel.fillStyle = 'blue';
        pincel.beginPath();
        pincel.arc(x, y, 10, 0, 2 * 3.14);
        pincel.fill();
        console.log(x + ',' + y);

    }

 tela.onclick = desenhaCirculo;

// desenha na tela no click / tela.onclick = desenhaCirculo;

// tiro a posica a mais do x tela.offsetLeft;
// tiro a posica a mais do y tela.offsetTop;


// Mudando as cores com o botao direito do mouse
// exemplo

<canvas width="600" height="400"></canvas>

<script>

  var tela = document.querySelector('canvas');
  var pincel = tela.getContext('2d');

  pincel.fillStyle = 'grey';
  pincel.fillRect(0, 0, 600, 400);

  var cores = ['blue', 'red', 'green']
  var indiceCorAtual = 0; // começa com blue

  function desenhaCirculo(evento) {

    var x = evento.pageX - tela.offsetLeft;
    var y = evento.pageY - tela.offsetTop;
    pincel.fillStyle = cores[indiceCorAtual];
    pincel.beginPath();
    pincel.arc(x, y, 10, 0, 2 * 3.14);
    pincel.fill();
    console.log(x + ',' + y);
  }

  tela.onclick = desenhaCirculo;

  function mudaCor() {

    indiceCorAtual++;
    if (indiceCorAtual >= cores.length) {
      indiceCorAtual = 0; // volta para a primeira cor, azul
    }
    return true; // para não exibir o menu padrão do canvas
  }

  tela.oncontextmenu = mudaCor;

</script>

// desenhando na tela

  tela.onmousemove = desenhaCirculo;

  tela.onmousedown = habilitaDesenhar;
  tela.onmouseup = desabilitaDesenhar;

// criando uma paleta de cores no mundo html
<br> Escolha uma cor <input type="color">
// conectando a paleta ao javascript
 var paleta = document.querySelector("input");
// conectando na funcão
 pincel.fillStyle = paleta.value;

// retorno de uma função
function somarDoisValores(valor1, valor2) {
    resultadoDaSoma = valor1 + valor2
    return resultadoDaSoma
}

console.log(somarDoisValores(200, 1000))

// Math.PI valor PI


// Limpar trecho
pincel.clearRect(0, 0, 600, 400);

// setInterval (nomedafunção, quantidade em milissegundos);
Quando queremos passar uma função como parâmetro para outra, precisamos omitir seus parênteses (), chamamos isso de callback em programação 

//retornandoCirculo
    var x = 20;
    var sentido = 1;

    function atualizaTela() {
        limpaTela();
        if (x > 600) {
          sentido = -1;
        } else if (x < 0) {
          sentido = 1;
        }
        desenhaCirculo(x, 20, 10);
        x = x + sentido;

    }

//A pergunta é: como podemos inverter um booleano? A resposta é através do operador lógico NOT, que é representado no mundo JavaScript pelo símbolo !


function trocaBandeira() {

    if(mostraBrasil) {
        desenhaBandeiraBrasil();
    } else {
        desenhaBandeiraAlemanha();
    }
    mostraBrasil = !mostraBrasil;
}

//Math.floor = aredodando número para baixo
return Math.floor(Math.random()






















